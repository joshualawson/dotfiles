# Makes aliases work with sudo
alias sudo="sudo "

# Neovim Aliases
alias vim="nvim"
alias vi="nvim"
alias v="nvim"

# Development function
dev() {
    cd ~/Development
    tmux new-session -A -s dev
}

# Update function
update() {
    yay -Syyuu --noconfirm
}
